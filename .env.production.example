# =============================================================================
# Expert Intelligence Platform - Production Environment Template
# Copy to .env.production and set values. Link all services (Neon, Grok, n8n, FastAPI).
# =============================================================================

# ---- Database (Neon PostgreSQL + pgvector) ----
DATABASE_URL="postgresql://USER:PASSWORD@HOST-POOLER/DBNAME?sslmode=require"
DIRECT_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# ---- PII Encryption (min 16 chars; rotate periodically) ----
PII_ENCRYPTION_KEY="your-32-char-or-longer-secret-key-here"
# Or: ENCRYPTION_KEY="..."

# ---- Embeddings (xAI Grok or OpenAI) ----
EMBEDDING_PROVIDER="xai"
XAI_API_KEY="xai-..."
# OPENAI_API_KEY="sk-..."   # when EMBEDDING_PROVIDER=openai

# ---- ML Microservice (FastAPI - AWS App Runner / ECS URL in prod) ----
ML_SERVICE_URL="https://your-ml-service.run.app"
# Local: http://localhost:8000

# ---- n8n (workflow orchestration) ----
N8N_BASE_URL="https://your-n8n-instance.com"
N8N_WEBHOOK_URL="https://your-n8n-instance.com/webhook/hunt"
N8N_API_KEY="n8n_api_..."

# ---- Webhook security (HMAC - same secret in n8n) ----
SHARED_SECRET="your-webhook-hmac-secret"
# Or: N8N_WEBHOOK_SECRET="..."

# ---- Clerk (Auth) ----
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_live_..."
CLERK_SECRET_KEY="sk_live_..."

# ---- Cron (Expiry Engine) ----
CRON_SECRET="your-cron-bearer-token"

# ---- App URL (for server-side callbacks / health checks) ----
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
