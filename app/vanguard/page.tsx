/**
 * Vanguard Command Center — Default view
 * 4-panel bento: Nav | Search-Filter | Data Engine | Inspector
 */
import { DataEngine } from '@/components/vanguard/DataEngine';

export default function VanguardPage() {
  return <DataEngine />;
}
